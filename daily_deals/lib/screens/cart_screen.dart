import 'package:flutter/cupertino.dart';

class CartScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("In cart screen", style: TextStyle(fontSize: 20),),
    );
  }

}